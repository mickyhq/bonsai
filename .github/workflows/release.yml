name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  binaries:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            name: linux-x64
            target: x86_64-unknown-linux-gnu
          - os: macos-latest
            name: macos-arm64
            target: aarch64-apple-darwin
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - name: Test
        run: cargo test
      - name: Build release binary
        run: cargo build --release --target "${{ matrix.target }}"
      - name: Package binary
        run: |
          mkdir -p dist
          cp "target/${{ matrix.target }}/release/bonsai" "dist/bonsai-${{ matrix.name }}"
          chmod +x "dist/bonsai-${{ matrix.name }}"
          shasum -a 256 "dist/bonsai-${{ matrix.name }}" > "dist/bonsai-${{ matrix.name }}.sha256"
      - name: Smoke packaged binary
        run: bash tests/release_binary_incremental_smoke.sh "dist/bonsai-${{ matrix.name }}"
      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: bonsai-${{ matrix.name }}
          path: |
            dist/bonsai-${{ matrix.name }}
            dist/bonsai-${{ matrix.name }}.sha256

  vsix:
    name: Package VSIX
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: copilot/bonsai-vscode/package-lock.json
      - name: Install extension dependencies
        run: npm ci
        working-directory: copilot/bonsai-vscode
      - name: Compile extension
        run: npm run compile
        working-directory: copilot/bonsai-vscode
      - name: Package VSIX
        run: npm run package
        working-directory: copilot/bonsai-vscode
      - name: Upload VSIX artifact
        uses: actions/upload-artifact@v4
        with:
          name: bonsai-vscode-vsix
          path: copilot/bonsai-vscode/bonsai-vscode-*.vsix

  publish:
    name: Publish GitHub release
    runs-on: ubuntu-latest
    needs:
      - binaries
      - vsix
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true
      - name: Smoke downloaded Linux binary
        run: |
          chmod +x dist/bonsai-linux-x64
          bash tests/release_binary_incremental_smoke.sh dist/bonsai-linux-x64
      - name: Write release notes
        run: |
          cat > release-notes.md <<'NOTES'
          ## Install without Rust

          macOS Apple Silicon:

          ```sh
          curl -L -o bonsai https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/bonsai-macos-arm64
          chmod +x bonsai
          ./bonsai --version
          ```

          Linux x64:

          ```sh
          curl -L -o bonsai https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/bonsai-linux-x64
          chmod +x bonsai
          ./bonsai --version
          ```

          Then run it in a repo:

          ```sh
          ./bonsai . --prompt --output clipboard
          ```

          SHA-256 checksum files are attached beside each binary.
          NOTES
      - name: Publish release assets
        uses: softprops/action-gh-release@v2
        with:
          body_path: release-notes.md
          files: dist/*
